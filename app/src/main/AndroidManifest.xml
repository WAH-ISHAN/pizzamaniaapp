<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">



    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />

    <!-- Mark hardware as optional so Play doesn't filter devices -->
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.location.network" android:required="false" />

    <application
        android:allowBackup="true"
        android:label="Pizza Mania"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Google Maps API key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_MAPS_API_KEY_HERE" />

        <!-- FileProvider (ensure res/xml/file_paths.xml exists) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.pizzamaniaapp.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- If you created a ContentProvider class, enable later
        <provider
            android:name="com.example.pizzamaniaapp.app.provider.MenuContentProvider"
            android:authorities="com.example.pizzamaniaapp.provider"
            android:exported="false" />
        -->

        <!-- Activities (adjust if your classes differ) -->
        <activity android:name="com.example.pizzamaniaapp.app.ui.map.BranchesMapActivity" />
        <activity android:name="com.example.pizzamaniaapp.app.ui.checkout.CheckoutActivity" />
        <activity android:name="com.example.pizzamaniaapp.app.ui.orders.OrderDetailActivity" />
        <activity android:name="com.example.pizzamaniaapp.app.ui.menu.ItemDetailActivity" />
        <activity android:name="com.example.pizzamaniaapp.app.ui.auth.ProfileActivity" />
        <activity android:name="com.example.pizzamaniaapp.app.ui.auth.RegisterActivity" />
        <activity android:name="com.example.pizzamaniaapp.app.ui.auth.LoginActivity" />

        <activity
            android:name="com.example.pizzamaniaapp.app.ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>